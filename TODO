web page output monitors 
fork new thread for performance monitor updates
email/sms notifications
replace SOURCE... in db init with reading in statements and executing them
config option to auto accept auto discovery entries

svc dependencies

display acks in device view
add group authz
device list return objects user has permission on
add permissions to objects
remove device from group
make right click select elements where there's a context menu
user preferences (theme, notifications, etc...)
